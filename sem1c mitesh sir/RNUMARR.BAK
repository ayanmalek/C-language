#include<stdio.h>
#include<conio.h>
void main()
{
    int i,a[5],no,find ;


    clrscr();

    printf("Enter how many number to input ");
    scanf("%d",&no);

    for(i=0;i<=no;i++)
    {
       printf(" Enter number [%d]:",i);
       scanf("%d",&a[i]);

    } for(i=0;i<=no;i++)
    {
       if( a[i]==find)
       a[i]=0;

    }
    for(i=0;i<=no;i++)
    {
	printf("\n value after replace is[%d]:%d",i , a[i]);

    }
    getch();
}