#include<stdio.h>
#include<conio.h>
int maxar(int);

void main()
{  int n,max;
clrscr();

	printf("Enter Number of Element");
	scanf("%d",&n);
	maxar(n);


getch();
}
int maxar(int n)
{
  int a[50];
  int i,max=0;


  for(i=0;i<n;i++){
    printf("\nEnter element value of a[%d]::",i);
    scanf("%d",&a[i]);

  }
  for(i=0;i<n;i++){
   printf("\nThe value of a[%d] is :: %d",i,a[i]);
  }
  for(i=0;i<n;i++){
   if (a[i]>max){
    max=a[i];
   }
  }

  printf("\nThe max of all array Element is ::%d",max);
 return max;
}